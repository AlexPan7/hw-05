{
	"name": "lesson-03",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"client-install": "cd client && pnpm install",
		"server-install": "cd server && pnpm install",
		"install-all-deps": "concurrently \"pnpm run server-install\" \"pnpm run client-install\"",
		"client-start": "cd client && pnpm run dev",
		"server-start": "cd server && pnpm run dev",
		"dev": "concurrently \"pnpm run client-start\" \"pnpm run server-start\""
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"concurrently": "^8.0.1"
	},
	"dependencies": {
		"react-quill": "^2.0.0"
	}
}
